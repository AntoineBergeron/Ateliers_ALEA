resultat <- vector("numeric")
names(resultat) <- vector("character")
n <- 1
for(etape in seq(-5, 5, 0.1)){
resultat[n] <- etape
names(resultat)[n] <- paste("Position ", n)
n <- n + 1
}
resultat
for(etape in c("Bonjour", "programmeurs", "en R")) {
print(etape)
}
for(etape in c(2,7,8)) {
print(etape)
}
piece <- c("pile","face")
combinaison = "face-face"
while(combinaison != "pile-pile") { # (!=)
essai1 <- sample(piece, 1)  # sample(piece, 2, replace = TRUE)
essai2 <- sample(piece, 1)
combinaison <- paste(essai1, "-" , essai2, sep = "")
print(combinaison)
}
piece <- c("pile","face")
combinaison = "face-face"
while(combinaison != "pile-pile") { # (!=)
essai1 <- sample(piece, 1)  # sample(piece, 2, replace = TRUE)
essai2 <- sample(piece, 1)
combinaison <- paste(essai1, "-" , essai2, sep = "")
print(combinaison)
}
essai <- sample(piece, 2, replace = T)  # sample(piece, 2, replace = TRUE)
piece <- c("pile","face")
combinaison = "face-face"
while(combinaison != "pile-pile") { # (!=)
essai <- sample(piece, 2, replace = T)  # sample(piece, 2, replace = TRUE)
combinaison <- paste(essai[1], "-" , essai[2], sep = "")
print(combinaison)
}
piece <- c("pile","face")
combinaison = "face-face"
while(combinaison != "pile-pile") { # (!=)
essai <- sample(piece, 2, replace = T)  # sample(piece, 2, replace = TRUE)
combinaison <- paste(essai[1], "-" , essai[2], sep = "")
print(combinaison)
}
